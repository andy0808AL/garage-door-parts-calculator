# Garage Door Parts Calculator

Single-page web app for AA Impact Garage Door. Calculates tracks, cables, springs, and prints labels.

Open `index.html` in a browser. Requires a local print server at `http://localhost:5000` for printing.
